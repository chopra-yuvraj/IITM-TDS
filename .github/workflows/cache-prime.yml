name: Final Combined Workflow

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: 24f2002642@ds.study.iitm.ac.in
        run: echo "Email step working"
        
      - name: prime-cache-53a55a9
        run: echo "Cache step working"
        
      - name: Setup cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/pip
          key: cache-53a55a9
