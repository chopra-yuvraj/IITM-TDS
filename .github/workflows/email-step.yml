name: Email Step Example

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 24f2002642@ds.study.iitm.ac.in
        run: echo "Hello, this is a named step!"
